import { StyleSheet } from "react-native";

export default StyleSheet.create({
  helpButton: {
    fontSize: 15,
    fontWeight: "300",
    letterSpacing: 1
  },
  modalView: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center"
  }
});
